<Window x:Class="CourseProjectDataBaseCars.DBLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CourseProjectDataBaseCars"
        mc:Ignorable="d"
        Title="DBLoginWindow" Height="340" Width="600">
    <Window.DataContext>
        <local:DBLoginWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="База данных Автодилер" FontSize="45" HorizontalAlignment="Center" Margin="0 14"/>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <!--<TextBox Text="Логин" TextAlignment="Center" VerticalContentAlignment="Center" FontSize="36"/>-->
            <TextBox Text="{Binding User.UserId}" FontSize="36" VerticalAlignment="Center" Margin="100 0" TextAlignment="Center"/>
            <TextBox Grid.Row="1" Text="{Binding User.Password}" VerticalAlignment="Center" Margin="100 0" TextAlignment="Center" FontSize="36"/>
        </Grid>

        <Grid Grid.Row="2" Margin="0 10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Command="{Binding LoginCommand}" Content="Войти" FontSize="30" Margin="30 0"/>
            <Button Command="{Binding CancelCommand}" Grid.Column="1" Content="Отмена" FontSize="30" Margin="30 0"/>
        </Grid>
    </Grid>
</Window>
