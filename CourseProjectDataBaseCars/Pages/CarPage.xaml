<local:BasePage x:TypeArguments="local:CarPageViewModel"
    x:Class="CourseProjectDataBaseCars.CarPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:CourseProjectDataBaseCars"
      mc:Ignorable="d"
      Height="600" Width="1200"
      x:Name="carPage"
      Title="CarPage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="0.3*"/>
            </Grid.ColumnDefinitions>

            <Button Command="{Binding PreviousPageCommand}" HorizontalAlignment="Left" FontSize="30" Content="X" VerticalAlignment="Center"/>
            <TextBlock Text="{Binding CurrentCar.Name, StringFormat={}Модель: {0}}" FontSize="35" VerticalAlignment="Center" HorizontalAlignment="Center"/>
            <Border Grid.Row="1" BorderThickness="1">
                <Border.BorderBrush>
                    <LinearGradientBrush StartPoint="0 0" EndPoint="1 1">
                        <GradientStop Color="#fff" Offset="0"/>
                        <GradientStop Color="#2B314D" Offset="0.5"/>
                        <GradientStop Color="#fff" Offset="1"/>
                    </LinearGradientBrush>
                </Border.BorderBrush>
            </Border>
            <TextBlock Grid.Row="2" Text="{Binding TotalCost, StringFormat={}Итог: {0} руб.}" FontSize="31" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="30 0 0 0"/>
            <TextBlock Grid.Row="2" Text="{Binding MonthlyPay, StringFormat={}Платёж: {0} руб.}" FontSize="31" VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0 0 30 0"/>

            <Border Grid.RowSpan="3" Grid.Column="1" Background="#eee" Padding="8">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>
                    <TextBlock Text="Забронировать" FontSize="30" Margin="0 20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    <Border Grid.Row="1" BorderThickness="1">
                        <Border.BorderBrush>
                            <SolidColorBrush Color="#000"/>
                        </Border.BorderBrush>
                    </Border>
                    <TextBox Grid.Row="2" VerticalAlignment="Center" Margin="20 0" FontSize="20"/>
                    <TextBox Grid.Row="3" VerticalAlignment="Center" Margin="20 0" FontSize="20"/>
                    <Button Grid.Row="4" Content="Продолжить" FontSize="25"/>
                </Grid>
            </Border>
            
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <ListBox Margin="5 0" ItemsSource="{Binding CreditItems}" SelectedIndex="{Binding SelectedCredit}">
                <ListBox.Template>
                    <ControlTemplate>
                        <DockPanel LastChildFill="True">
                            <Grid DockPanel.Dock="Top" TextBlock.FontSize="20" Name="carPageCredits">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Банк"/>
                                <TextBlock Grid.Column="1" Text="Ставка (%)"/>
                                <TextBlock Grid.Column="2" Text="Срок (год)"/>
                            </Grid>
                            <ScrollViewer HorizontalScrollBarVisibility="Disabled">
                                <ItemsPresenter></ItemsPresenter>
                            </ScrollViewer>
                        </DockPanel>
                    </ControlTemplate>
                </ListBox.Template>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Width="{Binding ActualWidth, ElementName=carPageCredits}" TextBlock.FontSize="23">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Bank.Name}" HorizontalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="{Binding Rate}" HorizontalAlignment="Center"/>
                            <TextBlock Grid.Column="2" Text="{Binding Expiration}" HorizontalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            <ListBox Grid.Column="1" Margin="5 0" ItemsSource="{Binding FactoryItems}" SelectedIndex="{Binding SelectedFactory}">
                <ListBox.Template>
                    <ControlTemplate>
                        <DockPanel LastChildFill="True">
                            <Grid DockPanel.Dock="Top" TextBlock.FontSize="20" Name="carPageFactories">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="Страна"/>
                                <TextBlock Grid.Column="1" Text="Город"/>
                                <TextBlock Grid.Column="2" Text="Доставка"/>
                                <TextBlock Grid.Column="3" Text="Время доставки"/>
                            </Grid>
                            <ScrollViewer HorizontalScrollBarVisibility="Disabled">
                                <ItemsPresenter></ItemsPresenter>
                            </ScrollViewer>
                        </DockPanel>
                    </ControlTemplate>
                </ListBox.Template>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Width="{Binding Path=ActualWidth, ElementName=carPageFactories}" TextBlock.FontSize="23">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Country}" HorizontalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="{Binding City}" HorizontalAlignment="Center"/>
                            <TextBlock Grid.Column="2" Text="{Binding TranspCost}" HorizontalAlignment="Center"/>
                            <TextBlock Grid.Column="3" Text="{Binding DeliveryTime}" HorizontalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>

        <Grid Grid.Row="2" Margin="0 15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Button Command="{Binding AddRefCommand}" 
                Content="Добавить связь" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" Width="210" Padding="0 10"/>
            <Button Command="{Binding DeleteCreditRefCommand}" 
                Grid.Column="1" Content="Удалить связь с кредитом" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" Width="250" Padding="0 10"/>
            <Button Command="{Binding DeleteFactoryRefCommand}" 
                Grid.Column="2" Content="Удалить связь с заводом" FontSize="20" VerticalAlignment="Center" HorizontalAlignment="Center" Width="250" Padding="0 10"/>
        </Grid>
        
    </Grid>
</local:BasePage>
